import React from 'react';
import { useState, useEffect } from '';

const header = () => {
  const array = funti.map(() => {});

  const views = (
    <div>
      <h1></h1>
    </div>
  );
};

export default header;
